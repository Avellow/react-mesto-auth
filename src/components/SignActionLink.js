import {useEffect, useState} from "react";
import {useLocation} from "react-router-dom";


function SignActionLink(props) {

    //может пригодиться в будущем

    return (
        <Link>
            {props.text}
        </Link>
    )
}

export default SignActionLink;